meta:
  version: 1.0
  environment: python
  toolchain:
    name: docker
    type: docker

services:
  web:
    image: python:3.11-slim
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000
    env:
      DEBUG: "False"
      ALLOWED_HOSTS: "*"
      DATABASE_URL: ""